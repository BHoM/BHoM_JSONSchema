{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "$id" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v9.3/SQLite_oM/Configs/DeleteConfig.json",
  "title" : "DeleteConfig",
  "type" : ["object", "null"],
  "description" : "DeleteConfig: Configuration for deleting objects from SQLite database with intelligent filtering capabilities.",
  "properties" : {
    "RequireFilterConditions" : {
      "type" : "boolean",
      "description" : "Whether to require at least one filter condition. If true, prevents accidental deletion of entire table. Default is true."
    },
    "MaxRowsToDelete" : {
      "type" : "integer",
      "description" : "Maximum number of rows to delete in a single operation. If 0, no limit is applied. Use with caution."
    },
    "DryRun" : {
      "type" : "boolean",
      "description" : "Whether to perform a dry run that returns the count of rows that would be deleted without actually deleting them."
    },
    "WrapNonBHoMObjects" : {
      "type" : "boolean",
      "description" : "If true, the Push action wraps any non-BHoM type into a BH.oM.Adapter.ObjectWrapper, allowing them to make use of the full Adapter workflow."
    },
    "AllowHashForComparing" : {
      "type" : "boolean",
      "description" : "If true and if no specific EqualityComparer is found for the type, Diffing hashes are computed and used to compare the objects."
    },
    "DiffingConfig" : {
      "description" : "Configurations for the Diffing hashing. Requires `AllowHashForComparing` to be set to true. DiffingConfig: General configurations for the Diffing process, including settings for the Hash computation.",
      "$ref" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v9.3/Diffing_oM/DiffingConfig.json"
    },
    "_t" : {
      "type" : "string",
      "description" : "Optional type disciminator.",
      "const" : "BH.oM.SQLite.Configs.DeleteConfig"
    },
    "_bhomVersion" : {
      "type" : ["string", "null"],
      "description" : "Optional version of BHoM used as part of automatic versioning and schema upgrades."
    }
  },
  "required" : ["RequireFilterConditions", "MaxRowsToDelete", "DryRun"]
}