{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "$id" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v8.3/Excel_oM/ExcelPushConfig.json",
  "title" : "ExcelPushConfig",
  "type" : ["object", "null"],
  "description" : "ExcelPushConfig: Configuration used for adapter interaction with Excel on Push action.",
  "properties" : {
    "Worksheet" : {
      "type" : ["string", "null"],
      "description" : "Name of the worksheet to write to."
    },
    "StartingCell" : {
      "description" : "The first cell that will be filled with the pushed objects, i.e. top-left cell of the populated space in the spreadsheet. CellAddress: Object representing a cell address: its column index as string and row index as integer.",
      "$ref" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v8.3/Excel_oM/CellAddress.json"
    },
    "ObjectProperties" : {
      "type" : ["array", "null"],
      "description" : "List of object properties to push to the table. Those will form the columns of the created table.",
      "items" : {
        "type" : ["string", "null"]
      }
    },
    "PropertiesToIgnore" : {
      "type" : ["array", "null"],
      "description" : "List of object properties that shouldn't be added to the table.",
      "items" : {
        "type" : ["string", "null"]
      }
    },
    "GoDeepInProperties" : {
      "type" : "boolean",
      "description" : "If true, non-primitive properties will be divided in multiple column."
    },
    "TransposeObjectTable" : {
      "type" : "boolean",
      "description" : "If true, transpose the table so that each column is a separate object and each row is a different property."
    },
    "IncludePropertyNames" : {
      "type" : "boolean",
      "description" : "If true, the property names will be shown as the first row (or column if transposed)"
    },
    "WorkbookProperties" : {
      "description" : "Properties to apply to workbook and contents. If not null, the meta information of the workbook will be updated on push. WorkbookProperties: Object representing the meta information of the workbook.",
      "$ref" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v8.3/Excel_oM/WorkbookProperties.json"
    },
    "WrapNonBHoMObjects" : {
      "type" : "boolean",
      "description" : "If true, the Push action wraps any non-BHoM type into a BH.oM.Adapter.ObjectWrapper, allowing them to make use of the full Adapter workflow."
    },
    "AllowHashForComparing" : {
      "type" : "boolean",
      "description" : "If true and if no specific EqualityComparer is found for the type, Diffing hashes are computed and used to compare the objects."
    },
    "DiffingConfig" : {
      "description" : "Configurations for the Diffing hashing. Requires `AllowHashForComparing` to be set to true. DiffingConfig: General configurations for the Diffing process, including settings for the Hash computation.",
      "$ref" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/v8.3/Diffing_oM/DiffingConfig.json"
    },
    "_t" : {
      "type" : "string",
      "description" : "Optional type disciminator.",
      "const" : "BH.oM.Adapters.Excel.ExcelPushConfig"
    },
    "_bhomVersion" : {
      "type" : ["string", "null"],
      "description" : "Optional version of BHoM used as part of automatic versioning and schema upgrades."
    }
  },
  "required" : ["Worksheet", "StartingCell", "ObjectProperties", "PropertiesToIgnore", "GoDeepInProperties", "TransposeObjectTable", "IncludePropertyNames", "WorkbookProperties"]
}