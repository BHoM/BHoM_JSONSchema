{
  "$id" : "https://raw.githubusercontent.com/BHoM/BHoM_JSONSchema/develop/Verification_oM/Conditions/Formula.json",
  "title" : "Formula",
  "type" : ["object", "null"],
  "description" : "Formula: Formula in a form of a string in a specific convention, used to calculate values in verification workflows.",
  "properties" : {
    "Equation" : {
      "type" : ["string", "null"],
      "description" : "Equation in a form of a string, resulting in either numerical, string or Boolean values.\nOnly basic mathematical operators are currently supported.\nStrings need to be wrapped in single quotation marks (').\nEnum values need to stay unwrapped and expressed only with the value itself (e.g. MyValue instead of MyEnum.MyValue).\nExamples of valid formule:\n(a + b) * 2\na == b * 2\na == 'StringValue'\na == EnumValue\na == 'StringValueA' or (a == 'StringValueB' and b == 'StringValueC')\nif a == 0: 'zero', else if a == 1: 'one', else if a == 2: 'two', else: 'not supported'\nif a == b and a == c: 'all values equal', else: 'values not equal'"
    },
    "Tolerance" : { },
    "_t" : {
      "type" : ["string", "null"],
      "description" : "Optional type disciminator.",
      "const" : "BH.oM.Verification.Conditions.Formula"
    },
    "_bhomVersion" : {
      "type" : ["string", "null"],
      "description" : "Optional version of BHoM used as part of automatic versioning and schema upgrades."
    }
  },
  "required" : ["Equation", "Tolerance"]
}